[pytest]
testpaths = tests
markers =
    slow: long-running benchmarks or integration checks
    requires_model: tests that need local AI model files
    requires_audio: tests that need local audio hardware or loopback devices
    network: tests that download datasets or models
